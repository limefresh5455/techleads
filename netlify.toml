[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Set in Netlify UI after deploying the backend (Render, etc.):
  # API_PROXY_URL = https://your-api.onrender.com
